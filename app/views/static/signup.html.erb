<div class="w3-container w3-padding-32">
  <div class="w3-card-4 w3-padding-large" style="max-width:600px; margin:auto;">
    <header class="w3-container w3-center w3-theme-d1 w3-padding-bottom"> <%# Assuming w3-theme-d1 is a dark accent from your theme %>
      <h1>Create Account</h1>
    </header>

    <%= form_for(@user, url: '/signup', html: { class: "w3-container w3-margin-top" }) do |f| %>
      <%# Display errors if any %>
      <% if @user && @user.errors.any? %>
        <div class="w3-panel w3-red w3-padding w3-margin-bottom">
          <span onclick="this.parentElement.style.display='none'" class="w3-button w3-red w3-large w3-display-topright">&times;</span>
          <h3><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h3>
          <ul>
            <% @user.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <p>
        <%= f.label :fname, 'First Name', class: "w3-text-theme" %>
        <%= f.text_field :fname, class: "w3-input w3-border w3-round" %>
      </p>

      <p>
        <%= f.label :lname, 'Last Name', class: "w3-text-theme" %>
        <%= f.text_field :lname, class: "w3-input w3-border w3-round" %>
      </p>

      <p>
        <%= f.label :email, class: "w3-text-theme" %>
        <%= f.text_field :email, class: "w3-input w3-border w3-round" %>
      </p>

      <p>
        <%= f.label :password, class: "w3-text-theme" %>
        <%= f.password_field :password, class: "w3-input w3-border w3-round" %>
      </p>

      <p>
        <%= f.label :password_confirmation, 'Confirm Password', class: "w3-text-theme" %>
        <%= f.password_field :password_confirmation, class: "w3-input w3-border w3-round" %>
      </p>

      <p class="w3-center w3-padding-16">
        <%= f.submit "Create Account", class: "w3-button w3-theme-d1 w3-round-large w3-large" %>
      </p>
    <% end %>

    <div class="w3-container w3-center w3-padding-16 w3-border-top">
      <p>Or:</p>
      <%= link_to '/auth/google_oauth2', class: "w3-button w3-theme-l4 w3-text-theme w3-border w3-border-theme w3-round-large" do %>
        <i class="fab fa-google"></i> Sign Up With Google <%# Assuming you have Font Awesome or similar for icons %>
      <% end %>
    </div>
  </div>
</div>

<%#
  Notes:
  - Replaced table with divs and W3.CSS classes.
  - Added a card-like container (`w3-card-4`) for the form.
  - Inputs are styled with `w3-input w3-border w3-round`.
  - Labels have `w3-text-theme` (you might need to adjust this based on your chosen theme's contrast).
  - Submit button is styled with `w3-button w3-theme-d1 w3-round-large w3-large`.
  - Google Sign Up is styled as an alternative button.
    - It uses an icon (fab fa-google). If you don't have Font Awesome, you can remove the <i> tag or use text.
  - Added a basic error display section, which is common for signup forms.
  - The header "Create Account" is now styled with a theme background.
  - Paragraph tags `<p>` are used to group labels and inputs for spacing.
%>
